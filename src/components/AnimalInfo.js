import React, { useState, useEffect } from 'react';

export default function AnimalInfo() {
    const [animalInfo, setAnimalInfo] = useState([]);

    useEffect(() => {
        
    }, []);

    return (
        <main className="animal-info-page">
            <h1>Animal Info Page</h1>
        </main>
    )
}
