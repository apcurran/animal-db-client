import React, { useState, useCallback } from 'react';

export default function AnimalAdd() {
    return (
        <div>
            <h1>Add New Animal</h1>
            <form className="form">
                
            </form>
        </div>
    )
}
